---
// src/components/Footer.astro
// No changes needed here; assuming you handle any dynamic data (e.g., social links) via props or imports.
---
<footer class="site-footer" role="contentinfo">
  <div class="footer-container container">
    
    <section class="footer-column footer-brand-info">
      <a href="/" class="footer-brand" aria-label="Go to BLKKK homepage">BLKKK</a>
      <p class="copyright">&copy; MMXXV // ALL RIGHTS RESERVED</p>
    </section>

    <nav class="footer-column footer-links" aria-labelledby="footer-info-heading">
      <h4 id="footer-info-heading">[ INFO ]</h4>
      <ul>
        <li><a href="/contact">CONTACT</a></li>
        <li><a href="/shipping">SHIPPING & RETURNS</a></li>
        <li><a href="/privacy">PRIVACY POLICY</a></li>
        <li><a href="/faq">FAQ</a></li>
      </ul>
    </nav>

    <section class="footer-column footer-subscribe" aria-labelledby="footer-subscribe-heading">
      <h4 id="footer-subscribe-heading">[ RECEIVE TRANSMISSIONS ]</h4>
      <form class="subscribe-form" action="/api/subscribe" method="POST" id="subscribe-form">
        <input 
          type="email" 
          name="email" 
          placeholder="ENTER YOUR EMAIL" 
          required 
          aria-describedby="subscribe-message"
          autocomplete="email"
        />
        <button type="submit" aria-label="Subscribe to newsletter">→</button>
      </form>
      <div id="subscribe-message" class="subscribe-message" aria-live="polite" hidden></div>
      <div class="social-links">
        <a href="#" aria-label="Follow us on Twitter" title="Twitter">
          <svg class="social-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M23 3a10.9 10.9 0 01-3.14 1.53 4.48 4.48 0 00-7.86 3v1A10.66 10.66 0 013 4s-4 9 5 13a11.64 11.64 0 01-7 2c9 5 20 0 20-11.5a4.5 4.5 0 00-.08-.83A7.72 7.72 0 0023 3z"/>
          </svg>
        </a>
        <a href="#" aria-label="Follow us on Instagram" title="Instagram">
          <svg class="social-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <rect x="2" y="2" width="20" height="20" rx="5" ry="5"/><path d="m16 11.37A4 4 0 1112.63 8 4 4 0 0116 11.37z"/><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/>
          </svg>
        </a>
        <a href="#" aria-label="Follow us on TikTok" title="TikTok">
          <svg class="social-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M12.525.02c1.31-.02 2.61-.01 3.91-.02.08 1.53.63 3.09 1.75 4.17 1.12 1.11 2.7 1.62 4.24 1.79v4.03c-1.44-.05-2.89-.35-4.2-.97-.57-.26-1.1-.59-1.62-.93-.01 2.92.01 5.84-.02 8.75-.08 1.4-.54 2.79-1.35 3.94-1.31 1.92-3.58 3.17-5.91 3.21-1.43.08-2.86-.31-4.08-1.03-2.02-1.19-3.44-3.37-3.65-5.71-.02-.5-.03-1-.01-1.49.18-1.9 1.12-3.72 2.58-4.96 1.66-1.44 3.98-2.13 6.15-1.72.02 1.48-.04 2.96-.04 4.44-.99-.32-2.15-.23-3.02.37-.63.41-1.11 1.04-1.36 1.75-.21.51-.15 1.07-.14 1.61.24 1.64 1.82 3.02 3.5 2.87 1.12-.01 2.19-.66 2.77-1.61.19-.33.4-.67.41-1.06.1-1.79.06-3.57.07-5.36.01-4.03-.01-8.05.02-12.07z"/>
          </svg>
        </a>
      </div>
    </section>
  </div>
</footer>

<style>
  /* Base footer styles */
  .site-footer {
    padding: clamp(3rem, 5vw, 4rem) 0;
    background-color: #050505;
    border-top: 1px solid var(--border-color);
  }

  .footer-container {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: clamp(2rem, 4vw, 3rem);
  }

  /* Brand and copyright */
  .footer-brand {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 900;
    letter-spacing: 3px;
    color: var(--text-main);
    text-transform: uppercase;
    transition: color 0.3s ease, transform 0.3s ease;
    display: inline-block;
  }
  .footer-brand:hover,
  .footer-brand:focus {
    color: var(--accent-primary);
    transform: translateY(-2px);
    outline: 2px solid var(--accent-primary);
    outline-offset: 2px;
  }

  .copyright {
    font-family: var(--font-mono);
    font-size: clamp(0.75rem, 1.5vw, 0.8rem);
    color: var(--text-muted);
    margin-top: 1rem;
    letter-spacing: 1px;
    opacity: 0.7;
  }

  /* Links section */
  .footer-column h4 {
    font-family: var(--font-mono);
    font-size: clamp(0.9rem, 2vw, 1rem);
    letter-spacing: 2px;
    color: var(--text-muted);
    margin-bottom: 1.5rem;
    font-weight: 400;
  }

  .footer-links ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
  }
  .footer-links a {
    font-family: var(--font-mono);
    font-size: clamp(0.85rem, 1.5vw, 0.9rem);
    color: var(--text-muted);
    letter-spacing: 1px;
    transition: color 0.3s ease, padding-left 0.3s ease, transform 0.3s ease;
    display: inline-block;
  }
  .footer-links a:hover,
  .footer-links a:focus {
    color: var(--accent-primary);
    padding-left: 5px;
    transform: translateX(5px);
    outline: 2px solid var(--accent-primary);
    outline-offset: 2px;
  }

  /* Subscribe form */
  .subscribe-form {
    display: flex;
    border: 1px solid var(--border-color);
    margin-bottom: 2rem;
    border-radius: 4px;
    overflow: hidden;
    transition: box-shadow 0.3s ease;
  }
  .subscribe-form:focus-within {
    box-shadow: 0 0 8px rgba(var(--accent-primary-rgb, 255, 255, 255), 0.3);
  }
  .subscribe-form input {
    flex-grow: 1;
    background: transparent;
    border: none;
    color: var(--text-main);
    padding: clamp(0.7rem, 2vw, 0.8rem);
    font-family: var(--font-mono);
    font-size: clamp(0.85rem, 1.5vw, 0.9rem);
    letter-spacing: 1px;
  }
  .subscribe-form input:focus {
    outline: none;
    background: #111;
  }
  .subscribe-form button {
    background: transparent;
    border: none;
    border-left: 1px solid var(--border-color);
    color: var(--text-muted);
    padding: 0 clamp(1rem, 2vw, 1.5rem);
    font-size: clamp(1.2rem, 2vw, 1.5rem);
    cursor: pointer;
    transition: background-color 0.3s ease, color 0.3s ease, transform 0.3s ease;
  }
  .subscribe-form button:hover,
  .subscribe-form button:focus {
    background-color: var(--accent-primary);
    color: #050505;
    transform: scale(1.05);
    outline: 2px solid var(--accent-primary);
    outline-offset: -2px;
  }

  /* Subscribe message (for feedback) */
  .subscribe-message {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    margin-bottom: 1rem;
    color: var(--text-muted);
  }
  .subscribe-message.success {
    color: var(--accent-primary);
  }
  .subscribe-message.error {
    color: #ff6b6b; /* Assuming a red for errors */
  }

  /* Social links */
  .social-links {
    display: flex;
    gap: clamp(1rem, 2vw, 1.5rem);
    align-items: center;
  }
  .social-icon {
    width: 24px;
    height: 24px;
    stroke: var(--text-muted);
    transition: stroke 0.3s ease, transform 0.3s ease;
  }
  .social-links a:hover .social-icon,
  .social-links a:focus .social-icon {
    stroke: var(--accent-primary);
    transform: scale(1.1);
  }
  .social-links a:focus {
    outline: 2px solid var(--accent-primary);
    outline-offset: 2px;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .site-footer {
      padding: clamp(2rem, 4vw, 3rem) 0;
    }
    .footer-container {
      text-align: center;
      gap: clamp(2rem, 4vw, 3rem);
    }
    .footer-links ul {
      align-items: center;
    }
    .subscribe-form {
      justify-content: center;
      max-width: 350px;
      margin-left: auto;
      margin-right: auto;
    }
    .social-links {
      justify-content: center;
    }
  }

  /* High contrast mode support */
  @media (prefers-contrast: high) {
    .footer-brand,
    .footer-links a,
    .social-icon {
      border: 1px solid currentColor;
    }
  }
</style>
